import 'package:flutter/material.dart';

class BlocErrorFragment extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text(
        'BLoC State error'
      ),
    );
  }
}